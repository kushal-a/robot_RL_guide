# Standalone Python for running streaming mode

In the Getting Started files py files, the script defaults to opening Isaac Sim in normal GUI mode. We have to modify that to instead full the full streaming mode.

Check 